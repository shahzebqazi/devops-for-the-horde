#!/usr/bin/env bash
# Copy to satellite repo as `nix/install.sh` (see devops-for-the-horde satellite-manifest.json).
set -euo pipefail
echo "nix/install.sh: replace this stub with your satellite install (e.g. home-manager, nix run .#sync)."
exit 0
